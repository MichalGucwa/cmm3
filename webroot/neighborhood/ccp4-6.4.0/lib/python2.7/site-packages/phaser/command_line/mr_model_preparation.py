# LIBTBX_SET_DISPATCHER_NAME phenix.mr_model_preparation
from phaser import mr_model_preparation
from libtbx.utils import Sorry
import sys

if __name__ == "__main__":
    
    try:
        mr_model_preparation.run( args = sys.argv[1:], out = sys.stdout )
        
    except Sorry:
        raise
    
    except Exception, e:
        print "An internal error has occurred"
        print "The traceback is as follows:"
        import traceback
        print traceback.format_exc()
        print "Terminating..."