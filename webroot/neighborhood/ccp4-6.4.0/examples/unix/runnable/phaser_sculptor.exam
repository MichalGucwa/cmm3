#!/bin/sh

# A short test for phaser.sculptor. Uses the toxd example

phaser.sculptor input.model.file_name=$CEXAM/toxd/toxd.pdb input.alignment.file_name=$CEXAM/toxd/alignment.pir output.folder=$CCP4_SCR
